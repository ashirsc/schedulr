import { MantineTheme, MantineThemeOverride, useMantineTheme } from '@mantine/core';

import React, { } from 'react';

export const AppTheme = React.createContext({ setTheme: (newTheme: MantineThemeOverride) => { } })
