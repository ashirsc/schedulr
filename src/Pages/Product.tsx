import React from "react";
import { Title } from "@mantine/core";

interface Props {}

export default function Product(props: Props) {
    return (
        <>
            <Title>Seemlessly run R scripts in the cloud</Title>
        </>
    );
}
